import 'package:flutter/material.dart';

// Placeholder — implementado no Chunk 8
class OrgansScreen extends StatelessWidget {
  const OrgansScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Organs')));
  }
}
