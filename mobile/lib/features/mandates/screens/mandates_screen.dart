import 'package:flutter/material.dart';

// Placeholder — implementado no Chunk 8
class MandatesScreen extends StatelessWidget {
  const MandatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Mandates')));
  }
}
